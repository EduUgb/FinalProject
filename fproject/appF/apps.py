from django.apps import AppConfig



class AppFConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'appF'
    verbose_name = 'Página de reservaciones'  # Aquí pones el nombre que deseas

